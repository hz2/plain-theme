<% if (post.categories && post.categories.length){ %>
  <div class="article-category"><svg class="svg"><use xlink:href="#i-folder-open"></use></svg>
    <%- list_categories(post.categories, {
      show_count: false,
      class: 'article-category',
      style: 'none',
      separator: ' / '
    }) %>
  </div>
<% } %>